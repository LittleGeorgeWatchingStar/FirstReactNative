import { useCallback } from 'react';
import { useFormikContext } from 'formik';

function useSubmitButton() {
  var _useFormikContext = useFormikContext(),
      submitForm = _useFormikContext.submitForm;

  var handlePress = useCallback(function (_ev) {
    submitForm();
  }, [submitForm]);
  return {
    onPress: handlePress
  };
}

export { useSubmitButton };
//# sourceMappingURL=formik-native.esm.js.map
